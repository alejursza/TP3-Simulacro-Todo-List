<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TP_simulacro">

        <!-- Activities para el Todo List -->
        <activity
            android:name=".MainTodoActivity"
            android:exported="true"
            android:label="Mis Tareas"
            android:theme="@style/Theme.TP_simulacro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.TP_simulacro" />

        <activity
            android:name=".CreateTaskActivity"
            android:exported="false"
            android:label="Crear Tarea"
            android:theme="@style/Theme.TP_simulacro" />

        <activity
            android:name=".EditTaskActivity"
            android:exported="false"
            android:label="Editar Tarea"
            android:theme="@style/Theme.TP_simulacro" />

        <activity
            android:name=".RemovedTaskActivity"
            android:exported="false"
            android:label="Tareas Eliminadas"
            android:theme="@style/Theme.TP_simulacro" />
    </application>

</manifest>